{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${mod_version}",

  "name": "${mod_name}",
  "description": "${mod_description}",
  "authors": ["${mod_authors}"],
  "contact": {
    "homepage": "${mod_site}",
    "sources": "${mod_source}",
    "issues": "${mod_issues}"
  },

  "license": "${mod_license}",
  "icon": "icon.png",

  "environment": "*",
  "entrypoints": {
    "fabric-datagen": [
      "dev.chililisoup.modularsynths.fabric.datagen.ModularSynthsDatagen"
    ],
    "main": [
      "dev.chililisoup.modularsynths.fabric.ModularSynthsFabric"
    ],
    "client": [
      "dev.chililisoup.modularsynths.fabric.client.ModularSynthsClientFabric"
    ]
  },
  "mixins": [
    "modularsynths.mixins.json",
    "modularsynths-common.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${fabric_loader_version}",
    "minecraft": "=${minecraft_version}"
  }
}
